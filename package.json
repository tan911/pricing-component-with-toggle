{
  "name": "pricing-component",
  "version": "1.0.0",
  "description": "pricing-component",
  "default": "index.html",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist",
    "deploy": "npm run build && git add . && git commit -a -m 'latest build' && git push origin main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.0",
    "parcel": "^2.8.0"
  }
}
